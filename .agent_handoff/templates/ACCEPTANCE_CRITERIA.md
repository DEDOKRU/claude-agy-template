# Acceptance criteria

Each item must be verifiable from the diff or by running a command.

1. <criterion — e.g. "GET /health returns 200 with {\"status\":\"ok\"}">
2. <criterion>
3. All verification commands from ANTIGRAVITY_PROMPT.md pass when run by the reviewer.
4. No files outside the allowed list in TASK_SPEC.md were modified.
